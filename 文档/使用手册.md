# AI Opportunity Radar — 使用手册

> **一句话**:每天早上自动从互联网 7 个数据源拉 AI 相关信号 → LLM 筛选 → 评分 → 深度研究 → 自动生产销售文案(Telegram / 闲鱼 / 小红书),**你的代码不是商品,你的代码是生产商品的机器**。

> **v2.0 改造目标**(见 [`继续文档.md`](继续文档.md)):
> 放弃 SaaS 化,聚焦"内容生产 + 多平台分销"。系统每天自动生产 10+ 条可销售内容,通过闲鱼 / 小红书 / 公众号 / 飞书完成销售,90 天内验证第一笔收入。

---

## 一、项目是什么

**AI Opportunity Radar v2.0** 是一个自托管的 **AI 商业情报生产系统**,每天 02:00 UTC 自动跑一遍完整流程,从公开互联网发现并评估 AI 产品机会,然后**自动生成可销售的内容**(不只是"通知"):*长文日报*(飞书/公众号)、*闲鱼商品 listing*、*小红书笔记*。

**完整流程**:
```
互联网 7 个数据源
  ↓ 抓取
RawItem(原始素材)
  ↓ LLM 筛选(MiniMax-M3)
Signal(有效信号)
  ↓ 确定性公式打分
Opportunity(0-100 分,带 8 个商业字段)
  ↓ 深度研究(LLM + Firecrawl + Browser Use)
ResearchReport(报告)
  ↓ 内容生成器(Phase 1 v2.0 新增)
   ├─ daily_report     → Markdown 长文 → 飞书 / 公众号
   ├─ xianyu_product   → JSON 商品    → 闲鱼(手动复制发布)
   └─ xiaohongshu_post → Markdown 笔记 → 小红书
  ↓ 飞书机器人推送(Phase 2 v2.0 新增)
   └─ 每日 02:30 UTC 自动推送日报卡片到飞书群
```

**6 个数据源**:`github` / `reddit` / `hackernews` / `producthunt` / `rss` / `youtube`

**回退链**:`Browser Use` → `Firecrawl` → `Mock`(任何一个挂了,自动降级到下一个,系统永远可用)

**8 个商业字段**(v2.0 新增在 `opportunities` 表):
- `target_customer` — 目标客户画像(企业级用户描述)
- `market_size` — 市场规模量级(如 `100M-500M USD`)
- `mvp_days` — 单人 MVP 估算天数
- `difficulty` — 实现难度(`easy` / `medium` / `hard`)
- `monetization_model` — 商业模式(如 `SaaS 订阅 49 USD/月`)
- `china_gap` — 中国市场空白点
- `content_status` — 内容生命周期(`new` → `generated` → `published` → `sold`)
- `commercial_status` — 销售资格(`unqualified` → `qualified` → `promising`)

---

## 二、架构一览

```
┌─────────────────────────────────────────────────────────────┐
│  你的电脑(Mac)                                              │
│                                                             │
│  docker compose up                                          │
│      ├─ postgres:16         数据库                          │
│      ├─ redis:7             缓存                            │
│      ├─ backend   :8000     FastAPI(主服务)                  │
│      ├─ frontend  :3000     Next.js 仪表盘                   │
│      ├─ worker             异步任务(目前是心跳)              │
│      └─ n8n       :5678     每日定时编排                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
              ↓ 调用托管 API
┌─────────────────────────────────────────────────────────────┐
│  外部托管服务                                                │
│  ├─ MiniMax(LLM,https://api.minimaxi.com/anthropic)         │
│  ├─ Firecrawl(网页抓取)                                     │
│  ├─ Browser Use(JS 渲染的页面)                              │
│  └─ Telegram Bot API(推送)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、环境要求

| 工具 | 最低版本 | 检查命令 |
|---|---|---|
| **Docker Desktop** | 4.x(Mac arm64) | `docker --version` |
| **Node.js**(可选,本地开发前端用) | 20+ | `node --version` |
| **Python**(可选,本地跑测试用) | 3.12+ | `python3 --version` |

你只需要 Docker Desktop。其他都跑在容器里。

---

## 四、第一次启动 — 6 步走完

### 步骤 1:克隆项目(假设已 clone,跳过)

### 步骤 2:填配置文件

```bash
cd /Users/jiang/development/AI\ Opportunity\ Radar
cp .env.example .env
```

打开 `.env`,**必须**填这几个(其他可以保持默认):

```bash
# LLM(必填)
MINIMAX_API_KEY=eyJhbGciOi...   # 你的 MiniMax 控制台 → API Keys
MINIMAX_API_URL=https://api.minimaxi.com/anthropic
MINIMAX_MODEL_STRONG=MiniMax-M3
LLM_DEFAULT_PROVIDER=minimax

# Telegram(必填,否则收不到推送)
TELEGRAM_BOT_TOKEN=123456:ABC...   # @BotFather → /newbot
TELEGRAM_CHAT_ID=987654321        # @userinfobot 查你的 ID

# 网页抓取(至少一个,推荐两个都填)
FIRECRAWL_API_KEY=fc-...          # firecrawl.dev
BROWSER_USE_API_KEY=bu-...        # browser-use.com

# 应用密钥(随便生成一个长字符串)
APP_SECRET_KEY=$(openssl rand -hex 32)
RADAR_WEBHOOK_SECRET=$(openssl rand -hex 16)

# n8n(可选,但推荐)
N8N_USER=admin@radar.local        # 必须是邮箱格式!
N8N_PASSWORD=你的密码               # 浏览器登录 n8n 的密码
```

### 步骤 3:启动整栈

```bash
make docker-up
```

第一次会拉镜像、装 Python 包、装 npm 包,大概 5-10 分钟。之后启动 < 30 秒。

### 步骤 4:验证服务都健康

```bash
curl -s http://localhost:8000/api/health | python3 -m json.tool
```

应该看到所有 7 个组件都是 `healthy`:
- `postgres` / `redis` / `llm` / `firecrawl` / `browser_use` / `telegram` / `n8n`

### 步骤 5:登录 n8n(只第一次)

打开 http://localhost:5678,用:
- Email: `admin@radar.local`
- Password: 你 `.env` 里设的 `N8N_PASSWORD`

进去后:
1. 左上角汉堡菜单 → **Settings** → **API** → 点 **Create an API key**
2. 复制生成的 key
3. 写进 `.env` 的 `N8N_API_KEY=...`
4. ```bash
   docker compose restart backend
   docker compose exec backend python -m scripts.n8n_sync --activate
   ```
   这会把 `n8n/workflows/` 下所有工作流(`daily-opportunity-discovery` / `manual-pipeline` / `research-opportunity` / `feishu-daily-digest`)推进 n8n 并自动激活。
5. 回到 n8n 面板刷新,看到所有工作流都是 active 状态 ✅

### 步骤 6:打开前端看仪表盘

http://localhost:3000 — 默认首页就是 dashboard。

---

## 五、3 个使用入口

| 入口 | 地址 | 主要用途 |
|---|---|---|
| **前端仪表盘** | http://localhost:3000 | 浏览所有机会、看详情、看研究报告、看图表 |
| **Telegram 机器人** | 你 Telegram 里和机器人聊天 | 接收每日推送摘要(主使用方式) |
| **n8n 面板** | http://localhost:5678 | 手动触发某个工作流、看执行历史、改定时 |

### 5.1 前端仪表盘(http://localhost:3000)

| 页面 | 路径 | 干什么的 |
|---|---|---|
| 首页/总览 | `/` | 今日热点、Top 机会卡片 |
| Dashboard | `/dashboard` | 图表:信号趋势、来源分布、评分分布 |
| 机会列表 | `/opportunities` | 所有历史机会,可以按分数/时间排序、过滤 |
| 机会详情 | `/opportunities/<id>` | 看某个机会的:原始来源、AI 评分依据、研究报告 |
| 研究详情 | `/research/<id>` | 看深度研究报告全文(LLM 生成的摘要 + 链接证据) |
| **Content Center** | `/content-center` | **v2.0 核心**:看系统自动生成的所有销售文案,直接复制到飞书/闲鱼/小红书/公众号发布 |
| **Orders** | `/orders` | **v2.0 Phase 4**:销售订单仪表盘,营收统计 + 状态流转(待发货→已发货→已确认) |
| **On-demand** | `/on-demand` | **v2.0 Phase 5**:输入 URL 或主题,即时生成七段式研究报告;可同时登记订单(¥299-¥999/份的售前场景) |
| 设置 | `/settings` | 改数据源启用状态、评分阈值、订阅偏好 |

### 5.2 Telegram 推送

系统**不会**作为常驻 bot 监听消息,而是每天 02:00 UTC 自动向你的 `TELEGRAM_CHAT_ID` 推送一张卡片:

```
🤖 AI 机会雷达 · 2026-08-27

5 个高分机会已生成,详情见仪表盘:radar://localhost:3000/opportunities

① ⭐ 92  GPT-5 Codex 插件市场开放第三方插件提交
② ⭐ 88  Anthropic 推出 Computer Use 商用版
③ ⭐ 85  ...
```

接收要求:
- 你已经和机器人**对话过一次**(否则 Telegram 不允许 bot 主动 sendMessage)
- `TELEGRAM_BOT_TOKEN` 和 `TELEGRAM_CHAT_ID` 正确

### 5.3 n8n 面板(http://localhost:5678)

4 个工作流:

| 工作流 | 触发方式 | 干什么 |
|---|---|---|
| `daily-opportunity-discovery` | **每天 02:00 UTC 自动** | 6 步全流程:发现 → 聚类 → 筛选 → 评分 → 研究 → 推送 Telegram |
| `manual-pipeline` | 手动点 "Execute Workflow" | 同上,但立刻跑,不等待定时 |
| `research-opportunity` | 手动点 + 传 ID | 只跑"深度研究"这一阶段(给某个具体机会补研究) |
| `feishu-daily-digest` | **每天 02:30 UTC 自动** | 拉 Top 5 机会 → 打成交互式卡片 → 推到飞书群 |

进工作流详情看流程图,所有 HTTP 节点调的是后端 `/api/internal/*` 端点(用 `RADAR_WEBHOOK_SECRET` 鉴权)。

---

## 五之二、v2.0 内容生产工作流(核心商业能力)

> **这是 v2.0 的核心价值** —— 上一节讲的是"看机会",这一节讲的是**自动产出可销售内容**。

### 5 个内容生成器

| 生成器 | 输出 | 销售渠道 | 用途 |
|---|---|---|---|
| `daily_report` | Markdown 长文 | 飞书机器人 / 公众号 | 完整日报,有标题/来源/痛点/市场/机会/行动 6 个固定章节 |
| `xianyu_product` | JSON 商品 listing | 闲鱼(手动复制发布) | 自动生成 ¥39-¥99 的数字商品,3-5 条卖点 + 定价建议 |
| `xiaohongshu_post` | Markdown 短文 | 小红书 | 300-600 字爆款笔记,带话题标签 + CTA 占位符 `{{CTA_URL}}` |

每个生成器:
- 输入 = `Opportunity` + 深度研究 `ResearchReport` + LLM Provider
- 输出 = `GeneratedContent`(标题 / 格式 / 内容 / 元数据 / 时间戳)
- 同时落库到 `notifications` 表(`channel` 字段对应销售渠道)

### 手动触发内容生成

```bash
# 跑 Top 5 的所有生成器
curl -X POST http://localhost:8000/api/internal/content/generate \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"limit": 5, "only_qualified": true}'

# 指定具体的机会 ID
curl -X POST http://localhost:8000/api/internal/content/generate \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"opportunity_ids": [12, 34, 56]}'

# 只跑某个生成器
curl -X POST http://localhost:8000/api/internal/content/generate \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"limit": 3, "generators": ["xianyu_product"]}'

# 跳过未达标的
curl -X POST http://localhost:8000/api/internal/content/generate \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"limit": 10, "only_qualified": false}'
```

> **关于 `X-Radar-Webhook`**:虽然 env 变量叫 `RADAR_WEBHOOK_SECRET`,但后端 `_check_webhook_secret()` 优先用 `APP_SECRET_KEY`(回退到 `RADAR_WEBHOOK_SECRET`)。生产建议两者都设置成同一个长随机串。

返回示例:
```json
{
  "generated_count": 15,
  "error_count": 0,
  "enriched_count": 5,
  "errors": []
}
```

### 查看已生成的内容

```bash
# 列出最近 50 条生成记录(按 channel 过滤)
curl 'http://localhost:8000/api/internal/notifications/history?limit=50&channel=xianyu' \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>"
```

返回的每条 `payload` 字段就是完整的 GeneratedContent,可以解析后直接复制到对应平台。

### 扩展:加新的内容生成器

只需要:
1. 在 `backend/app/services/content_generator/` 下新建一个文件,实现 `ContentGenerator` ABC
2. 文件末尾调用 `register(MyGenerator())` 自注册
3. 容器重启(开发模式 `--reload` 自动生效)

不需要改任何已有文件。

---

## 五之三、飞书机器人(Phase 2 v2.0)

> 这是 v2.0 的**企业情报交付通道** —— 每天早上 02:30 UTC 把昨日评分最高的 5 个机会打成一张交互式卡片,推到飞书群。

### 配置飞书自定义机器人

1. 在飞书群里,**设置 → 群机器人 → 添加机器人 → 自定义机器人**
2. 选"自定义机器人",设个名字,**关键:打开"加签"安全设置**(可选,推荐打开)
3. 复制 **Webhook URL**(形如 `https://open.feishu.cn/open-apis/bot/v2/hook/<token>`)
4. 如果开了加签,复制 **签名校验 secret**
5. 填进 `.env`:

```bash
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxx
FEISHU_WEBHOOK_SECRET=你的加签 secret  # 留空 = 不签名
FEISHU_TIMEOUT=15.0
```

6. 重启后端:
```bash
docker compose restart backend
```

### 收到的卡片长这样

```
┌─────────────────────────────────────────────┐
│ 🔥 AI 机会雷达日报 · 2026-08-27               │  ← 蓝色 header
├─────────────────────────────────────────────┤
│ 今日发现 5 个机会,平均评分 87.5 / 100         │
│ ───────────────────                          │
│ **TOP 1 · ⭐ 92**                            │
│ **AI 法律合同审核**                           │
│ 市场:100M-500M USD · MVP:14 天 · 难度:medium │
│ 海外律师事务所在用 LLM 自动审核合同条款。      │
│ [查看详情](http://localhost:3000/opportunities/1) │
│ ───────────────────                          │
│ **TOP 2 · 🟢 85**                            │
│ **AI 客服质量分析**                           │
│ ...                                          │
├─────────────────────────────────────────────┤
│ 数据来自 GitHub / Reddit / HN / PH / RSS / YT │
└─────────────────────────────────────────────┘
```

### 自动推送

每天 02:30 UTC(主流程 02:00 UTC 跑完后 30 分钟),n8n 自动调用:
- 工作流:`feishu-daily-digest`
- 调后端:`POST /api/internal/feishu/digest/send`

如果飞书 webhook 没配,后端自动降级到 mock provider,**不会报错**,测试时也能照常触发。

### 手动触发

```bash
# 立即推一次日报(默认最近 24 小时 Top 5)
curl -X POST http://localhost:8000/api/internal/feishu/digest/send \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"limit": 5, "only_qualified": true, "window_hours": 24}'

# 全历史 Top 10(给潜在客户演示用)
curl -X POST http://localhost:8000/api/internal/feishu/digest/send \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"limit": 10, "window_hours": null}'

# 自动先跑内容生成器再推送(确保卡片背后的内容已落库)
curl -X POST http://localhost:8000/api/internal/feishu/digest/send \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"limit": 5, "auto_generate_content": true}'
```

返回示例:
```json
{
  "sent": true,
  "opportunity_count": 5,
  "window_start": "2026-08-26T12:30:00+00:00",
  "window_end":   "2026-08-27T12:30:00+00:00",
  "error": null,
  "provider": "feishu",
  "body_chars": 1823,
  "content_generated": 5
}
```

### 加签模式

如果飞书群机器人开了"加签",后端会在 body 里自动附加:
```json
{
  "msg_type": "interactive",
  "card": {...},
  "timestamp": "1700000000",
  "sign": "<base64(HMAC-SHA256(key=secret, msg=\"ts\\nsecret\"))>"
}
```
算法在 `backend/app/services/feishu/base.py:sign_feishu_payload()`,有完整测试覆盖,匹配飞书官方参考实现。

---

## 五之四、Content Center 内容生产中心(Phase 3 v2.0)

> **这是一个 UI 工作台** —— 把内容生产器(Phase 1)和飞书推送(Phase 2)产生的内容**集中到一个页面**,让人可以一眼看到每个机会对应 4 个渠道的销售文案,并跟踪发布/成交状态。

### 打开

http://localhost:3000/content-center

### 它长什么样

每个高分机会一行,从左到右 4 张卡片(对应 4 个销售渠道),顶端有状态徽章和两个操作按钮:

```
┌─────────────────────────────────────────────────────────────────────┐
│ AI 法律合同审核                                  [✓ 已发布]          │
│ 评分 87.5 · 商业 qualified · 市场 100M-500M USD · MVP 14 天         │
│ 中国空白:中国律所市场分散,微信小程序是关键渠道                      │
│                                                  [标记已发布] [标记已售出] │
├──────────────┬──────────────┬──────────────┬──────────────────────┤
│ 飞书         │ 闲鱼         │ 小红书        │ 公众号                │
│ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │ ┌──────────────────┐ │
│ │ # 今日AI │ │ │ ¥49      │ │ │ 国外一个 │ │ │ 尚未生成。          │ │
│ │ 商业机会 │ │ │ 100 个AI │ │ │ AI 月入 │ │ │ 先到 n8n 触发     │ │
│ │ ...      │ │ │ 机会     │ │ │ 5 万美元 │ │ │ 内容生产。          │ │
│ └──────────┘ │ └──────────┘ │ └──────────┘ │ └──────────────────┘ │
│  [复制]      │  [复制]      │  [复制]      │                       │
└──────────────┴──────────────┴──────────────┴──────────────────────┘
```

### 4 个渠道

| 渠道 | 内容格式 | 用途 |
|---|---|---|
| **飞书** | Markdown 长文(完整日报) | 飞书群里看的机会情报 |
| **闲鱼** | JSON listing(标题/价格/卖点) | 数字商品,¥39-¥99 自动定价 |
| **小红书** | Markdown 短文(300-600 字) | 笔记风格,带话题标签 |
| **公众号** | Markdown 长文(预留位) | 1 周 / 1 月复盘型深度文章 |

### 操作流程

1. **看** — 系统每天 02:00 UTC 跑完发现流程后,**自动**跑内容生成器生成所有 4 个渠道内容(由 Phase 1 的 `/api/internal/content/generate` 端点触发,通常在 n8n `daily-opportunity-discovery` 末尾)
2. **复制** — 点卡片右上角"复制"按钮,内容到剪贴板,粘贴到对应平台发布
3. **标记已发布** — 发布到任意平台后,点"标记已发布"按钮,系统把 `content_status` 从 `generated` 改成 `published`
4. **标记已售出** — 真正卖出商品(闲鱼成交、付费阅读、订阅等)后,点"标记已售出"按钮,系统把 `content_status` 改成 `sold`,并把 `commercial_status` 升级为 `promising`

### 状态机

```
new (尚未生成)
  ↓ n8n 跑内容生成器
generated (已生成,可复制)
  ↓ 用户点"标记已发布"
published (已发布到外部平台)
  ↓ 用户点"标记已售出"
sold (已成交)  →  用于统计 ROI
```

每次状态变化都走后端,前端只是显示。状态变化会自动同步到:
- Dashboard 统计卡片
- Telegram / 飞书 每日推送(高亮已售出机会)
- `notifications` 表(留痕审计)

### 过滤参数

URL 上加参数:
```
/content-center?only_qualified=false&limit=50
```
- `only_qualified` — 默认 `true`,只显示 `commercial_status=qualified` 的高潜机会。改成 `false` 看全部。
- `limit` — 默认 20,最多显示多少行机会。

### 手动触发生成内容

如果想**立刻**给某些机会生成内容(不等 n8n 凌晨):
```bash
curl -X POST http://localhost:8000/api/internal/content/generate \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"limit": 10, "only_qualified": true}'
```
然后刷新 Content Center 页面,新内容就出现了。

### 相关后端端点

| 方法 | 路径 | 干什么 |
|---|---|---|
| GET | `/api/internal/content/by_opportunity` | 列出每个机会的最近一份各渠道内容 |
| POST | `/api/internal/content/{opportunity_id}/mark_published` | 把机会标记为已发布 |
| POST | `/api/internal/content/{opportunity_id}/mark_sold` | 把机会标记为已售出 |
| POST | `/api/internal/content/generate` | 手动跑内容生成器(Phase 1) |

### 默认 n8n 工作流编排

```
02:00 UTC daily-opportunity-discovery
   ├─ 发现 → 聚类 → 筛选 → 评分 → 研究
   └─ 自动跑内容生成器(生成 4 渠道内容)
02:30 UTC feishu-daily-digest
   └─ 拉最新 Top 5 机会 → 推飞书卡片
```

### 为什么不用 Tiptap / Notion 那种富文本编辑器?

**故意保持简单**。Content Center 只做三件事:看、复制、改状态。富文本编辑器需要数据库 schema 迁移、协同锁、版本控制,这些都不是 v2.0 的商业价值(90 天第一笔收入)。复制到外部平台用平台自己的编辑器发布,反而避免重复维护。

---

## 五之五、商业订单管理(Phase 4 v2.0)

> **这是 v2.0 商业闭环的最后一环** —— Content Center 让你知道"哪些内容可以卖",这一节让你追踪"实际卖出去了多少、钱收没收到、有没有发货"。

### 数据模型

```
orders(订单表) — v2.0 新增
  ├─ id
  ├─ opportunity_id → opportunities.id   锚定到具体机会
  ├─ customer_name              客户姓名 / 昵称
  ├─ customer_contact           联系方式(微信/手机/邮箱,可选)
  ├─ amount_cny                 销售金额(Decimal 10,2,精确到分)
  ├─ channel                    销售渠道(闲鱼/小红书/微信/公众号/飞书群/直接/其他)
  ├─ payment_method             支付方式(微信/支付宝/闲鱼担保/银行/其他)
  ├─ payment_reference          支付凭证号(交易号/闲鱼订单号)
  ├─ delivery_status            发货状态(pending → delivered → confirmed / refunded)
  ├─ commercial_status_snapshot 销售时机会的商业状态快照(用于历史分析)
  ├─ notes                      备注
  ├─ created_at / updated_at
```

### 3 个核心字段(对应 `继续文档.md` Phase 4 章节)

| 字段 | 含义 | 业务价值 |
|---|---|---|
| `customer_name` | 客户身份 | 复购 / 推荐追溯 |
| `amount_cny` | 金额 | 营收核算 |
| `delivery_status` | 发货状态 | 履约追踪 |

### 完整状态机

```
   客户下单
      ↓
   创建 Order(delivery_status=pending,amount_cny=X)
      ↓
   手动发货 / 发送数字资产
      ↓
   pending → delivered (点击表格"→ 已发货")
      ↓
   客户确认收货
      ↓
   delivered → confirmed (点击"→ 已确认" / 计入已成交数)
      ↓
   ───────────
   异常分支:
      → refunded  (退款,closed-loop,opportunity 仍 sold)
      → cancelled (取消,下错单/重复订单)
```

### 创建订单:3 种方式

**方式 1:从 Content Center 创建(最常用)**

1. 打开 http://localhost:3000/content-center
2. 找到要标记为已售出的机会行
3. 点击"标记已售出"按钮 → 弹出订单对话框
4. 填写客户姓名(必填)、金额、渠道、支付方式、备注
5. 点"确认记录" → 一键完成订单创建 + 机会标 sold

> 这种方式最适合"刚刚在闲鱼/小红书卖出一笔"的场景。

**方式 2:直接调 API**

```bash
curl -X POST http://localhost:8000/api/internal/orders \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "opportunity_id": 1,
    "customer_name": "张三",
    "customer_contact": "wechat:zhangsan",
    "amount_cny": 49,
    "channel": "xianyu",
    "payment_method": "wechat",
    "payment_reference": "xy-2026-0001",
    "notes": "客户要求 PDF 版"
  }'
```

**方式 3:Phase 3 mark_sold 接口带 order**

```bash
# 一个调用 = 创建订单 + 标记已售出
curl -X POST http://localhost:8000/api/internal/content/1/mark_sold \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{
    "order": {
      "customer_name": "李四",
      "amount_cny": 99,
      "channel": "wechat"
    }
  }'
```

### 订单管理面板

http://localhost:3000/orders

顶部 4 张统计卡片:**总订单 · 总营收 · 已成交(已发货+已确认) · 待发货**

下面按渠道分解(闲鱼 / 小红书 / 微信 / 公众号 / 飞书群 / 直接 / 其他)显示各渠道订单数和营收。

下面是订单表格,可以:
- 按渠道筛选
- 按发货状态筛选
- 一键推进状态(→ 已发货 / → 已确认)
- 跳转到对应的机会详情页

### 推进订单状态

```bash
# pending → delivered
curl -X POST http://localhost:8000/api/internal/orders/1/status \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"delivery_status": "delivered"}'

# delivered → confirmed
curl -X POST http://localhost:8000/api/internal/orders/1/status \
  -d '{"delivery_status": "confirmed"}'

# → refunded (退款)
curl -X POST http://localhost:8000/api/internal/orders/1/status \
  -d '{"delivery_status": "refunded"}'
```

### 营收统计

```bash
curl http://localhost:8000/api/internal/orders/stats \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>"
```

返回示例:
```json
{
  "total_orders": 12,
  "total_revenue_cny": 738.0,
  "delivered_count": 5,
  "confirmed_count": 3,
  "pending_count": 4,
  "by_channel": [
    {"channel": "xianyu", "count": 7, "revenue_cny": 343.0},
    {"channel": "xiaohongshu", "count": 3, "revenue_cny": 195.0},
    {"channel": "wechat", "count": 2, "revenue_cny": 200.0}
  ],
  "by_delivery_status": {
    "pending": 4,
    "delivered": 5,
    "confirmed": 3
  }
}
```

### 相关后端端点

| 方法 | 路径 | 干什么 |
|---|---|---|
| POST | `/api/internal/orders` | 创建订单(默认同时标记机会为 sold) |
| GET | `/api/internal/orders` | 列表 + 过滤(channel, delivery_status, opportunity_id) |
| GET | `/api/internal/orders/stats` | 营收 + 按渠道 / 按状态聚合 |
| GET | `/api/internal/orders/{id}` | 单订单详情 |
| POST | `/api/internal/orders/{id}/status` | 更新 `delivery_status` |

### Phase 3 ↔ Phase 4 兼容性

`POST /content/{id}/mark_sold` 接口被扩展:
- **空 body 或没有 `order` 字段** → 老行为(只翻 flag,不创建订单),Phase 3 调用方零修改。
- **body 带 `{"order": {...}}`** → 新行为(创建订单 + 翻 flag),响应多一个 `order` 字段。

Phase 3 的 26 + 12 = 38 个旧测试全部继续通过;Phase 4 新增 23 个测试覆盖新行为。

### 故意不做的事

- ❌ 在线支付 / Stripe / 微信支付 API 集成 — 你和客户之间直接收钱,系统只记账
- ❌ 用户系统 / 多租户 — 一台机器一个管理员,见 `继续文档.md` "七、增加管理员模式"
- ❌ 发票 / 税务 / 退款流程 — 用闲鱼/小红书/微信交易自带的能力
- ❌ 复杂的 CRM 流程(跟进 / 漏斗 / 自动营销) — 90 天验证第一笔收入阶段不必要
- ❌ 微信支付回调监听 — 太复杂且与闲鱼/小红书平台不一致

### 给运营者的提醒

- **数字商品发货**:`delivery_status` 适合"手动发链接"型(扫码发 PDF、闲鱼发网盘链接)。
- **数字资产发货**:可以一次创建多笔订单,每笔对应一个客户的链接。
- **退款闭环**:`refunded` 状态保留订单记录(用于审计),不影响机会的 `content_status='sold'`(高水位标记)。

---

## 五之六、按需深度研究报告(Phase 5 v2.0)

> v2.0 的 **Product 2:AI 创业分析服务**(¥299-¥999/份)。客户付钱后,运营者输入一个公开 URL 或一个主题,系统会用与日常流水线**完全相同**的抓取 + LLM 流程即时生成七段式研究报告,并把销售作为订单登记 —— 全程一次 HTTP 调用完成。

### 设计动机

日常的"discovery → screening → scoring → research"流水线是**主动的**:每天 02:00 UTC 跑一次,从公开互联网里*发现*机会。按需报告是**被动的**:客户点名要看某个项目,我们要*立即*给一份同等深度的分析。

实现上:
1. 用客户的输入(URL 或主题)直接生成一个**临时 Opportunity**(slug = `on-demand-<uuid16>`)。
2. 把这个 URL/主题作为 **seed URL** 传给 `ResearchService.process_job(seed_urls=...)` —— **不**走发现→筛选打分那条慢路。
3. 走与日常流水线完全相同的:抓取 → LLM 七段式合成 → 落库 `research_reports`。
4. 如果请求里带了客户信息,**同一调用**创建 Order 并把机会标记为 `content_status='sold'`、`commercial_status='promising'`。

### 入口

| 名称 | 路径 | 说明 |
|---|---|---|
| **On-demand** | `/on-demand` | 输入 URL 或主题,生成报告;同时登记订单 |

顶部导航的 **On-demand** 入口直达。

### API

```bash
# 1. URL 模式:生成报告
curl -X POST http://localhost:8000/api/internal/research/on_demand \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com/ai-product"}'

# 2. 主题模式:系统会先用主题搜索种子 URL,再开始研究
curl -X POST http://localhost:8000/api/internal/research/on_demand \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"topic": "AI 法律合同审核"}'

# 3. URL + 客户信息:一次调用同时生成报告 + 登记订单(¥299)
curl -X POST http://localhost:8000/api/internal/research/on_demand \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com/paid-report",
    "customer_name": "李四",
    "customer_contact": "wechat:lisi",
    "amount_cny": 299,
    "channel": "wechat",
    "payment_method": "wechat",
    "notes": "first paid report"
  }'

# 4. 最近任务列表(默认 20 条)
curl -s "http://localhost:8000/api/internal/research/on_demand/recent?limit=20" \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>"

# 5. 单份报告完整详情(含全部 7 段 + 来源)
curl -s "http://localhost:8000/api/internal/research/on_demand/7" \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>"
```

#### 请求体约束(`OnDemandResearchRequest`)

- `url` 和 `topic` **二选一**,不能同时给也不能同时不给。
- URL 模式:只填 `url`,后端用它直接抓取。
- 主题模式:只填 `topic`,后端先用主题在 WebDataProvider 上做 search,拿到的 URL 作为 seed。
- 订单字段:**任何**一个客户/支付字段被设置,`customer_name` 和 `amount_cny` 都必填(其他字段:contact / channel / payment_method / payment_reference / notes 都可选)。

#### 响应体(`OnDemandCreateResponse`)

```json
{
  "opportunity_id": 100,
  "opportunity_title": "https://example.com/ai-product",
  "opportunity_slug": "on-demand-a1b2c3d4e5f6g7h8",
  "job_id": 7,
  "status": "completed",
  "recommendation": "recommend",
  "confidence": 0.78,
  "sources_count": 3,
  "executive_summary": "…",
  "order_id": 88
}
```

注意:接口是**同步**返回 —— 报告生成完才回包。如果怕超时,后续 Phase 可以拆成 `job_id` 异步模式。

### 前端页面(`/on-demand`)

1. **种子切换**:URL / 主题。
2. **可选附加订单**:`同时记录一笔订单` 复选框 —— 勾选后展开客户、联系方式、销售渠道、金额、备注字段。
3. **生成报告**:点击后内联展开七段式报告(执行摘要、市场分析、竞争分析、中国市场分析、变现分析、MVP 分析、风险分析)+ 参考资料链接。
4. **最近任务**:下方表格按时间倒序列出所有按需生成的任务,带推荐、置信度、来源数、状态徽章。

### 与日常流水线的关系

| | 日常流水线 | 按需报告 |
|---|---|---|
| 触发 | n8n 02:00 UTC 自动 | 运营者手动(UI/API) |
| 种子来源 | RawItem / OpportunitySource | 客户输入的 URL 或主题 |
| Opportunity 来源 | discovery → screening → scoring | 直接 bootstrap 一个 `(on-demand)` slug |
| Research 流程 | 完全相同 | 完全相同(`seed_urls` 覆盖) |
| 订单 | 不产生 | 可选,同一调用创建 |
| `content_status` | generated / published / sold | 生成后直接 → sold(若带订单) |

### 关键设计取舍

- **复用 `ResearchService` 而不是写一份新的**:任何抓取/LLM 流程的 bug fix 都自动惠及按需路径。代价是 `process_job` 多了一个 `seed_urls` 参数 + `_gather_sources` 一个 if 分支,极少侵入。
- **同调用创建订单**:运营者在客户付款后只点一次"生成报告"。`order_id` 在响应里直接返回,UI 也能立刻高亮&ldquo;订单 #88 已登记&rdquo;。
- **slug 带 `on-demand-` 前缀**:让 `GET /research/on_demand/recent` 能用 `Opportunity.summary LIKE '(on-demand)%'` 一句话过滤,不需要在表上加 `kind` 列。
- **不写新表**:`ResearchJob` + `ResearchReport` + `Order` 三张现有表已经覆盖所有需求。`on-demand` 只是一个 bootstrap 模式,不是一个新实体。

---

## 六、怎么手动触发一次完整流程

最快的方法是直接调后端 API:

```bash
# 触发完整发现 → 推送流程(从发现到 Telegram)
curl -X POST http://localhost:8000/api/internal/discovery/run \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"sources": ["github","reddit","hackernews","producthunt","rss","youtube"]}'

# 只跑某个阶段
curl -X POST http://localhost:8000/api/internal/research/run \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"limit": 5}'

# 立即推送今日摘要到 Telegram
curl -X POST http://localhost:8000/api/internal/notifications/digest/send \
  -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" \
  -d '{"max_entries": 5, "min_score": 70.0}'
```

可用端点:`/discovery/run`、`/clustering/run`、`/screening/run`、`/scoring/run`、`/research/run`、`/notifications/digest/send`

或者直接在 n8n 面板里点 `manual-pipeline` 工作流 → Execute Workflow,效果一样。

---

## 七、常用运维命令

所有命令都在项目根目录跑:

```bash
# === 启动 / 停止 ===
make docker-up              # 启动全部容器(首次会 build)
make docker-down            # 停止(保留所有数据)
make docker-logs            # tail 所有容器日志
docker compose logs -f backend    # 只看后端

# === 测试 ===
make test-backend           # 410 个后端测试
make test-frontend          # 前端测试
make lint                   # ruff + eslint

# === 数据库 ===
make backup                 # 导出 Postgres → backend/backups/radar-<时间>.sql
make backup-dry             # 看看会执行什么,不会真跑
make restore                # 恢复(--file=路径)
make restore-dry            # 同上 dry-run

# === n8n ===
make n8n-validate           # 校验 n8n/workflows/*.json 合法
make n8n-sync               # 把工作流推到运行中的 n8n + 激活
make n8n-sync -- --dry-run  # 只校验不推

# === 监控 ===
make metrics-scrape         # curl /api/metrics 看 radar_* 指标
curl -s http://localhost:8000/api/health/ready | python3 -m json.tool   # 严格就绪探针

# === 清理(慎用) ===
docker compose down -v      # 删容器 + 删所有数据
make clean                  # 清本地缓存
```

---

## 八、配置项速查

完整列表见 `.env.example`,这里只列最常用的:

| 变量 | 默认 | 含义 |
|---|---|---|
| `MINIMAX_API_KEY` | `""` | MiniMax 控制台拿 |
| `MINIMAX_API_URL` | `https://api.minimaxi.com/anthropic` | **不要改** |
| `MINIMAX_MODEL_STRONG` | `MiniMax-M3` | 主力模型 |
| `MINIMAX_MODEL_CHEAP` | `MiniMax-M2.7-highspeed` | 省钱用 |
| `LLM_DEFAULT_PROVIDER` | `minimax` | 默认用哪家 LLM |
| `TELEGRAM_BOT_TOKEN` | `""` | @BotFather 创建 bot 拿到 |
| `TELEGRAM_CHAT_ID` | `""` | @userinfobot 查你自己的 ID |
| `FEISHU_WEBHOOK_URL` | `""` | 飞书自定义机器人的完整 Webhook URL(含 token) |
| `FEISHU_WEBHOOK_SECRET` | `""` | 加签 secret,留空不签名 |
| `FIRECRAWL_API_KEY` | `""` | firecrawl.dev |
| `BROWSER_USE_API_KEY` | `""` | browser-use.com |
| `ENABLED_SOURCES` | `github,reddit,...` | 启用的数据源,逗号分隔 |
| `EMBEDDING_CLUSTER_THRESHOLD` | `0.82` | 聚类相似度阈值,越大越严格 |
| `RADAR_WEBHOOK_SECRET` | 必填 | 内部 API 鉴权 secret |
| `APP_SECRET_KEY` | 必填 | 通用密钥 |
| `MOCK_EXTERNAL_SERVICES` | `false` | 改成 `true` 让外部调用全部走 mock,纯本地开发用 |
| `APP_LOG_LEVEL` | `INFO` | `DEBUG` / `INFO` / `WARNING` / `ERROR` |
| `APP_DEBUG` | `false` | 改 `true` 看 SQL 查询、自动重载 |

---

## 九、故障排除

### "firecrawl health: down" / "browser_use health: down"

**原因**:对应 API key 没填、过期、或被代理拦截。

**检查**:
```bash
# 直接 curl 测试连通性
curl -H "Authorization: Bearer $FIRECRAWL_API_KEY" https://api.firecrawl.com/v1/health
curl -H "X-Browser-Use-API-Key: $BROWSER_USE_API_KEY" https://api.browser-use.com/api/v2/tasks -X POST -d '{"task":"test"}' -H "Content-Type: application/json"
```

**修法**:
- 检查 `.env` 里的 key 是否正确
- 如果用了代理,确认代理开着(`export https_proxy=...`)
- 系统会自动降级到下一个 provider,不影响整体运行

### Telegram 没收到推送

**检查 4 件事**:
1. 你和机器人**说过至少一次话**(发 `/start`)
2. `TELEGRAM_BOT_TOKEN` 和 `TELEGRAM_CHAT_ID` 在 `.env` 里填对了
3. 后端日志里有没有 `telegram_send_failed`
4. 测试推送:```bash
   curl -X POST http://localhost:8000/api/internal/notifications/test \
     -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" -d '{}'
   ```

### 飞书没收到卡片

**检查 5 件事**:
1. `.env` 里 `FEISHU_WEBHOOK_URL` 是不是 `https://open.feishu.cn/open-apis/bot/v2/hook/` 开头
2. 群机器人的"加签"开关 和 `FEISHU_WEBHOOK_SECRET` 是否一致(都开 / 都不开)
3. 后端日志里有没有 `feishu_send_failed`,看具体 `error`
4. 测试推送(不需真有飞书 webhook,会走 mock 记录):```bash
   curl -X POST http://localhost:8000/api/internal/feishu/digest/send \
     -H "X-Radar-Webhook: <你的 APP_SECRET_KEY>" -d '{"limit":3,"window_hours":null}'
   ```
5. 加签算法:签错的话飞书会返回 `StatusCode: 230001 / 230002`,看 `app/services/feishu/base.py:sign_feishu_payload` 对照

### n8n 工作流显示 401

API key 没填对。在 n8n 面板生成新的,写进 `.env` 的 `N8N_API_KEY`,然后:
```bash
docker compose restart backend
docker compose exec backend python -m scripts.n8n_sync --activate
```

### 后端启动报错 "pydantic_settings.exceptions.SettingsError"

通常是 `cors_allow_origins` 或 `enabled_sources` 这种逗号分隔字段出问题。看错误信息,一般是把列表写成了 JSON 格式而不是逗号分隔。

### 容器反复重启

```bash
docker compose ps                              # 看哪些不健康
docker compose logs backend --tail=50         # 看具体报错
```

最常见 3 个原因:
1. Postgres 还没就绪(等几秒就好,depends_on 已配)
2. `.env` 里某个必填字段没填
3. 端口被占用(改 `docker-compose.yml` 里的 ports)

### 想完全清空从零开始

```bash
docker compose down -v              # 删容器 + 删 volumes(包括 Postgres 数据)
rm -rf backend/backups/*.sql        # 清备份
make docker-up                      # 重新启动
```

---

## 十、开发模式(可选)

如果想改代码后热重载,容器里已经默认开了 `--reload`。直接改 `backend/app/*` 或 `frontend/src/*` 文件就生效。

跑测试不用启动 Docker:
```bash
cd backend && pip install -e ".[dev]" && pytest -q
cd frontend && npm install && npm test
```

---

## 十一、安全提醒

- **`.env` 文件永远不要 commit**(已经在 `.gitignore` 里了)
- **`.env` 不要在公共场合贴出来**(含真实 key)
- 默认 `CORS_ALLOW_ORIGINS=*`,生产环境改成具体域名
- 默认 `APP_DEBUG=false`,生产环境保持
- `RADAR_WEBHOOK_SECRET` 和 `APP_SECRET_KEY` 必须**用长随机字符串**

---

## 十二、相关链接

- 项目说明: [`README.md`](README.md)
- 部署运维: [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md)
- 监控告警: [`docs/RUNBOOK.md`](docs/RUNBOOK.md) + [`docs/ALERTS.md`](docs/ALERTS.md)
- 架构详解: [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)
- 第三方审计: [`docs/THIRD_PARTY_AUDIT.md`](docs/THIRD_PARTY_AUDIT.md)

---

> 最后更新:2026-08-27 — Phase 4 v2.0 商业订单管理(订单 / 营收 / 状态机)。本手册由项目开发者维护。如有改动同步更新。